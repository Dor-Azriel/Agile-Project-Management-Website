pipeline {
    agent { docker { image 'python:3.1.7' } }
    stages {
        stage('build') {
            steps {
                sh '
        source bin/activate
        pip install -r requirements.txt
        deactivate
       '
            }
        }
    }
}

